Dạ em đưa lên git ko đc hay chưa biết cách đưa nên em bỏ vô này ạ:((
 
mới đầu là cấu hình maven:
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example</groupId>
    <artifactId>servlet-login-demo</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <dependencies>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>5.0.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>




em bỏ cái web.xml cấu hình Servlet trong src vô :
<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Cookie Login Servlets -->
    <servlet>
        <servlet-name>CookieLoginServlet</servlet-name>
        <servlet-class>CookieLoginServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>CookieLoginServlet</servlet-name>
        <url-pattern>/cookie-login</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>CookieWelcomeServlet</servlet-name>
        <servlet-class>CookieWelcomeServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>CookieWelcomeServlet</servlet-name>
        <url-pattern>/cookie-welcome</url-pattern>
    </servlet-mapping>

    <!-- Session Login Servlets -->
    <servlet>
        <servlet-name>SessionLoginServlet</servlet-name>
        <servlet-class>SessionLoginServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>SessionLoginServlet</servlet-name>
        <url-pattern>/session-login</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>SessionProfileServlet</servlet-name>
        <servlet-class>SessionProfileServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>SessionProfileServlet</servlet-name>
        <url-pattern>/session-profile</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>SessionLogoutServlet</servlet-name>
        <servlet-class>SessionLogoutServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>SessionLogoutServlet</servlet-name>
        <url-pattern>/session-logout</url-pattern>
    </servlet-mapping>
</web-app>



cookie-login.html form login với Cookie:
<!DOCTYPE html>
<html>
<head>
    <title>Login with Cookie</title>
</head>
<body>
    <h2>Login Form (Cookie)</h2>
    <form action="cookie-login" method="post">
        Username: <input type="text" name="username"><br>
        Password: <input type="password" name="password"><br>
        <input type="submit" value="Login">
    </form>
</body>
</html>



xử lý CookieLoginServlet.java :
import jakarta.servlet.ServletException;
import jakarta.servlet.http.Cookie;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.io.PrintWriter;

public class CookieLoginServlet extends HttpServlet {
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html;charset=UTF-8");
        PrintWriter out = resp.getWriter();

        String username = req.getParameter("username");
        String password = req.getParameter("password");

        if (username.equals("tanvinh") && password.equals("123")) {
            out.println("Đăng nhập thành công!");
            Cookie cookie = new Cookie("username", username);
            cookie.setMaxAge(30); // Cookie hết hạn sau 30 giây
            resp.addCookie(cookie);
            out.println("<a href='cookie-welcome'>Đi đến trang chào mừng</a>");
        } else {
            out.println("Tài khoản hoặc mật khẩu không đúng!");
            req.getRequestDispatcher("cookie-login.html").include(req, resp);
        }
    }
}


code cookie chào mừng cookieWelcomeServlet.java
import jakarta.servlet.ServletException;
import jakarta.servlet.http.Cookie;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.io.PrintWriter;

public class CookieWelcomeServlet extends HttpServlet {
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html;charset=UTF-8");
        PrintWriter out = resp.getWriter();

        String name = "";
        Cookie[] cookies = req.getCookies();
        if (cookies != null) {
            for (Cookie c : cookies) {
                if (c.getName().equals("username")) {
                    name = c.getValue();
                    break;
                }
            }
        }

        if (!name.isEmpty()) {
            out.println("Chào " + name + "!");
        } else {
            out.println("Vui lòng đăng nhập trước.");
            resp.sendRedirect("cookie-login.html");
        }
    }
}

session-login.html ở đây mục from login với session 

<!DOCTYPE html>
<html>
<head>
    <title>Login with Session</title>
</head>
<body>
    <h2>Login Form (Session)</h2>
    <form action="session-login" method="post">
        Username: <input type="text" name="username"><br>
        Password: <input type="password" name="password"><br>
        <input type="submit" value="Login">
    </form>
</body>
</html>

CÒn code này xử lý login với session em có đăng nhập tên tanving pass 123 :
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import jakarta.servlet.http.HttpSession;

import java.io.IOException;
import java.io.PrintWriter;

public class SessionLoginServlet extends HttpServlet {
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html;charset=UTF-8");
        PrintWriter out = resp.getWriter();

        String username = req.getParameter("username");
        String password = req.getParameter("password");

        if (username.equals("tanvinh") && password.equals("123")) {
            out.println("Đăng nhập thành công!");
            HttpSession session = req.getSession();
            session.setAttribute("username", username);
            out.println("<a href='session-profile'>Đi đến trang profile</a>");
        } else {
            out.println("Tài khoản hoặc mật khẩu không đúng!");
            req.getRequestDispatcher("session-login.html").include(req, resp);
        }
    }
}


còn cái này vô là hiện chào tên : 
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import jakarta.servlet.http.HttpSession;

import java.io.IOException;
import java.io.PrintWriter;

public class SessionProfileServlet extends HttpServlet {
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html;charset=UTF-8");
        PrintWriter out = resp.getWriter();

        HttpSession session = req.getSession(false);
        if (session != null) {
            String name = (String) session.getAttribute("username");
            if (name != null) {
                out.println("Chào " + name + "!");
                out.println("<a href='session-logout'>Logout</a>");
                return;
            }
        }

        out.println("Vui lòng đăng nhập trước.");
        resp.sendRedirect("session-login.html");
    }
}





Dạ ko biết cái này ko đúng ý thầy nhưng em mới học web lần đầu mấy môn kia các bạn nhóm trưởng đưa lên git nên em chưa rành để e cải thiện ạ 